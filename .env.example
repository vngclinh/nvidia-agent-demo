# Sao chép file này thành .env rồi điền giá trị thật.
#   cp .env.example .env

# NVIDIA NIM API key (lấy miễn phí tại https://build.nvidia.com)
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Port cho Invoice MCP server
MCP_PORT=8765
